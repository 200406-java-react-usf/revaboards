export interface UserSchema {
    id: number,
    username: string,
    password: string,
    first_name: string,
    last_name: string,
    email: string,
    role_name: string
}

export interface ThreadSchema {
    // TODO provide thread schema
}

export interface PostSchema {
    // TODO provide post schema
}